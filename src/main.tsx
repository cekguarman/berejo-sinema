import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './berejo_sinema' // Sesuaikan dengan nama file .tsx baru Anda di folder src/
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
